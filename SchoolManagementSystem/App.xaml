<Application x:Class="SchoolManagementSystem.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SchoolManagementSystem">
    <Application.Resources>
        <Style TargetType="Button" x:Key="baseBtn">
            <Setter Property="Background" Value="#95a5a6"/>
            <Setter Property="Foreground" Value="WhiteSmoke"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Width" Value="auto"/>
            <Setter Property="Height"  Value="auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="FontSize" Value="22"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="FontWeight" Value="ExtraBold"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button" BasedOn="{StaticResource baseBtn}" x:Key="btnAdd">
            <Setter Property="Command" Value="{Binding Add}"/>
            <Setter Property="Content" Value="Add"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Default">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Add">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Edit">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Selected">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button" BasedOn="{StaticResource baseBtn}" x:Key="btnEdit">
            <Setter Property="Command" Value="{Binding Edit}"/>
            <Setter Property="Content" Value="Edit"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Default">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Add">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Edit">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Selected">
                    <Setter Property="IsEnabled" Value="True"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button" BasedOn="{StaticResource baseBtn}" x:Key="btnSave">
            <Setter Property="Command" Value="{Binding Save}"/>
            <Setter Property="Content" Value="Save"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Default">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Add">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Edit">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Selected">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource baseBtn}" x:Key="btnCancel">
            <Setter Property="Command" Value="{Binding Cancel}"/>
            <Setter Property="Content" Value="Cancel"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Default">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Add">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Edit">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Selected">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource baseBtn}" x:Key="btnDelete">
            <Setter Property="Command" Value="{Binding Delete}"/>
            <Setter Property="Content" Value="Delete"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Default">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituation}" Value="Add">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Edit">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentSituatin}" Value="Selected">
                    <Setter Property="IsEnabled" Value="True"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
